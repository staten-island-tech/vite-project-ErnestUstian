import "./style.css";
import { food } from "./Food";
import {DOM} from "./DOM";

const AllFunctions = {
    MainMenu: function () {
        DOM.MainMenu.insertAdjacentElement(
            "beforeend", 
            `
            `

        )
    }
}